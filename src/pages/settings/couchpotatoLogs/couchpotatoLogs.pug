div()

    table(class="w-full table-fixed border border-gray-300 bg-white")
            thead()
                tr(class="border-b border-gray-300 bg-gray-300")
                    th(class="w-auto md:w-32 px-4 py-2 whitespace-no-wrap border-b border-gray-300 text-md text-left leading-5") Logfil
                    th(class="w-auto md:w-8 py-2 whitespace-no-wrap border-b border-gray-300 text-md text-left leading-5 text-right")
                        
            tbody()
                tr(v-for="(log, index) in logs")
                    td(class="px-4 py-2 whitespace-no-wrap border-b border-gray-300 text-md leading-5 capitalize") {{ log }}
                    td(class="py-2 whitespace-no-wrap border-b border-gray-300 px-4 text-md leading-5 flex justify-end") 
                        button(class="btn mr-2" @click="openLog(log)")
                            font-awesome-icon(class="mr-1" icon="eye")
                            span(class="hidden md:inline") Öppna

                        button(class="btn" @click="deleteLog(log)")
                            font-awesome-icon(class="mr-1" icon="trash")
                            span(class="hidden md:inline") Radera