html()
    head
        meta(charset="utf-8")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        title() Couchpotato Editor
        
    body(class="bg-gray-100 p-8")
        div(id="app")
            router-view(:key="$route.fullPath")